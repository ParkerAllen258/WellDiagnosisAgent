# 项目协作约定

## 用户技术背景与决策方式

- 用户自述为技术小白，不能被要求直接提供具体技术方案、框架选型、工程配置或实现细节。
- 遇到任何需要用户决定的技术事项时，助手必须先整理 2–3 个可行方案。
- 每个方案必须使用通俗语言说明：
  - 它是什么；
  - 对本项目有什么影响；
  - 主要优点；
  - 主要缺点或代价。
- 助手必须明确给出推荐方案及推荐理由，但最终选择由用户决定。
- 技术问题应一次聚焦一个决策，避免把多个不相关的技术选择同时抛给用户。
- 不得只向用户询问“想用什么技术”“具体怎么实现”等需要专业背景才能回答的问题。

## Git 提交约定

- 所有 Git commit message 必须使用中文。
- 提交信息应简洁描述本次变更，例如：`新增异常诊断页面`、`修复任务筛选状态同步问题`。

## Git 分支与推送约定

- 远端仓库 `origin` 使用 SSH 地址：`git@github.com:ParkerAllen258/WellDiagnosisAgent.git`。
- 项目如果需要分阶段开发，每个开发阶段开始前创建独立分支，并在该分支上完成编码、测试和提交。
- 不得直接在 `main` 分支上进行阶段性功能开发。
- 阶段分支名由助手根据阶段内容直接确定，无需再向用户询问；默认采用 `feature/phase-阶段号-英文范围` 的清晰格式。
- 未收到用户明确的 `git push` 指令时，助手不得向任何远端仓库推送。
- 即使本地阶段已完成或已经提交，也只能报告状态并等待用户决定是否推送。
- 收到推送指令后，只能推送用户明确指定的单一分支；不得使用 `git push --all`、`git push --mirror`，也不得擅自推送标签。

## 本地规划文件约定

- 助手生成的规划、研究发现、执行进度和 superpowers 规格/计划文件仅供本地协作使用，不得推送到远端仓库。
- 以下路径必须保持 Git 忽略：
  - `/task_plan.md`
  - `/findings.md`
  - `/progress.md`
  - `/docs/superpowers/`
- Git 暂存和提交前必须检查文件清单，确保提交只包含项目代码、项目说明和用户要求保留的项目文件。
